-- No schema change: the site notice lives in the settings table, which
-- already exists. This file keeps the journal and the deployed database in
-- step for the release that introduced it.
SELECT 1;
